Linux(Ubuntu)

```bash
apt update

apt install python3.10-venv -y

## 创建虚拟环境
python3 -m venv .venv

## 激活虚拟环境
source .venv/bin/activate

## 安装三方库
(.venv) root@weijing:/workdir/proj/frida# pip install frida-tools
```

